# re0 运行时凭据。所有服务地址在 config/re0.yaml 中配置，此处仅放敏感凭据。
# 所有服务应为内网地址；本文件不引用任何公网 URL。

# 自托管 vLLM 等 OpenAI 兼容服务的 token（若未开启鉴权可留空）
VLLM_TOKEN=

# MySQL 只读账号密码
DB_PASSWORD=

# 外部 HTTP SQL 代理（若使用 db.type=http）
SQL_PROXY_TOKEN=

# 可选：覆盖配置文件路径
# RE0_CONFIG=config/re0.yaml
